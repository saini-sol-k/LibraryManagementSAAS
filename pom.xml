<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.librarysaas</groupId>
  <artifactId>library-saas-backend</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>Library SaaS Backend</name>
  <description>Multi-tenant Library &amp; Study Center Business Management SaaS Backend</description>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.2.2</version>
    <relativePath/>
  </parent>

  <properties>
    <java.version>21</java.version>
  </properties>
  <dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-bom</artifactId>
            <version>2.0.5</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>
  <dependencies>
    <!-- Web, JPA, Security, Validation, Actuator -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <!-- Flyway + MySQL -->
    <!-- Flyway -->
	<dependency>
	    <groupId>org.flywaydb</groupId>
	    <artifactId>flyway-core</artifactId>
	</dependency>
	
	<!-- Flyway MySQL support -->
	<dependency>
	    <groupId>org.flywaydb</groupId>
	    <artifactId>flyway-mysql</artifactId>
	</dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <version>8.0.33</version>
    </dependency>

    <!-- Lombok -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <!-- OpenAPI/Swagger -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>2.1.0</version>
    </dependency>

    <!-- Devtools (runtime, optional) -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-devtools</artifactId>
      <scope>runtime</scope>
      <optional>true</optional>
    </dependency>

    <!-- H2 for tests and dev -->
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
    </dependency>

    <!-- Test starter -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <groupId>org.junit.vintage</groupId>
          <artifactId>junit-vintage-engine</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <!-- JWT (io.jsonwebtoken) -->
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>0.11.5</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>0.11.5</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>0.11.5</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Align commons-lang3 to a version that provides ArrayFill (required by
         commons-compress used by Testcontainers). Upgrade to 3.20.0 so the
         runtime class is available. Kept default (compile) scope so transitive
         users see the same version. -->
    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-lang3</artifactId>
      <version>3.20.0</version>
    </dependency>
    <!-- Rely on Testcontainers BOM to provide docker-java and transport
         implementations. Removed manual docker-java transport override to
         avoid mixing incompatible docker-java versions with the BOM. -->
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
	<!-- Testcontainers for MySQL integration tests -->
    <dependency>
      	<groupId>org.testcontainers</groupId>
      	<artifactId>testcontainers</artifactId>
      	<scope>test</scope>
    </dependency>
    	
    <dependency>
    	<groupId>org.testcontainers</groupId>
    	<artifactId>testcontainers-mysql</artifactId>
    	<scope>test</scope>
    </dependency>

    <!-- Use Testcontainers' bundled docker-java artifacts (do not force a different
         docker-java version here). For compatibility with Testcontainers 1.21.3,
         allow its dependency management to select the matching docker-java
         modules. This avoids mixing incompatible docker-java versions which can
         result in requests using an incorrect Docker API path. -->

	
  <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-junit-jupiter</artifactId>
      <scope>test</scope>
  </dependency>
    <!-- No manual docker-java transport dependencies here — the testcontainers
         BOM manages docker-java and transports. If a transport is required in
         future, add it only if the BOM does not provide a compatible option. -->
  </dependencies>

   <build>
     <plugins>
       <plugin>
         <groupId>org.apache.maven.plugins</groupId>
         <artifactId>maven-compiler-plugin</artifactId>
         <version>3.11.0</version>
         <configuration>
           <release>${java.version}</release>
         </configuration>
       </plugin>

       <!-- Configure Surefire to run tests in-process so Testcontainers uses the same
            environment (useful on Windows named-pipe Docker setups where the forked
            JVM may not inherit or access the Docker endpoint correctly). This is a
            minimal, reversible change that does not alter production code or test
            semantics. Remove or revert if undesired. -->
       <plugin>
         <groupId>org.apache.maven.plugins</groupId>
         <artifactId>maven-surefire-plugin</artifactId>
         <version>3.1.2</version>
         <configuration>
           <!-- Run tests in-process (no fork) to avoid extra JVMs consuming host RAM.
                This reduces total memory pressure on Docker Desktop so MySQL
                containers are less likely to be OOM-killed. This is test-only. -->
           <forkCount>0</forkCount>
         </configuration>
       </plugin>

       <plugin>
         <groupId>org.springframework.boot</groupId>
         <artifactId>spring-boot-maven-plugin</artifactId>
       </plugin>
     </plugins>
   </build>

   <!-- Test profiles for different test scenarios -->
   <profiles>
     <!-- Profile for running integration tests with Testcontainers (MySQL) -->
     <profile>
       <id>test</id>
       <activation>
         <property>
           <name>!skipTests</name>
         </property>
       </activation>
       <properties>
         <maven.test.skip>false</maven.test.skip>
       </properties>
     </profile>

     <!-- Profile for running only unit tests (no Docker/Testcontainers) -->
     <profile>
       <id>unit-tests</id>
       <build>
         <plugins>
           <plugin>
             <groupId>org.apache.maven.plugins</groupId>
             <artifactId>maven-surefire-plugin</artifactId>
             <version>3.1.2</version>
             <configuration>
               <excludesFile>test-integration-excludes.txt</excludesFile>
               <forkCount>0</forkCount>
             </configuration>
           </plugin>
         </plugins>
       </build>
     </profile>

     <!-- Profile for quick validation (skips all tests) -->
     <profile>
       <id>build-only</id>
       <properties>
         <maven.test.skip>true</maven.test.skip>
       </properties>
     </profile>
   </profiles>
</project>